{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "start:prod": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development nodemon index.js",
    "test": "cross-env NODE_ENV=test mocha tests/*.test.js",
    "test:coverage": "cross-env NODE_ENV=test nyc mocha tests/*.test.js",
    "test:watch": "cross-env NODE_ENV=test mocha tests/*.test.js --watch",
    "test:server": "cross-env NODE_ENV=test nodemon index.js",
    "db:connect:dev": "cross-env NODE_ENV=development node -e \"import('./src/config/database.js').then(db => db.testConnection())\"",
    "db:connect:test": "cross-env NODE_ENV=test node -e \"import('./src/config/database.js').then(db => db.testConnection())\"",
    "db:connect:prod": "cross-env NODE_ENV=production node -e \"import('./src/config/database.js').then(db => db.testConnection())\"",
    "db:setup": "cross-env NODE_ENV=development node scripts/createTestDatabase.js",
    "db:setup:test": "cross-env NODE_ENV=test node scripts/createTestDatabase.js",
    "config:validate": "node -e \"import('./src/config/config.js').then(c => c.default.validateConfig() && console.log('✅ Configuration valid'))\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "dotnet": "^1.1.4",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "nodemon": "^3.1.10",
    "pg": "^8.16.3",
    "xml2js": "^0.6.2"
  },
  "devDependencies": {
    "chai": "^6.0.1",
    "cross-env": "^10.0.0",
    "mocha": "^11.7.2",
    "supertest": "^7.0.0"
  }
}
